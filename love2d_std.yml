# LuaJIT is mostly based on Lua 5.1
base: lua51
globals:
  love:
    # TODO
    any: true
  # FIXME: incorrect
  utf8.char:
    args:
      - required: utf8.char should be used with an argument despite it not throwing
        type: number
      - required: false
        type: "..."
  utf8.charpattern:
    property: read-only
  utf8.codepoint:
    args:
      - type: string
      - required: false
        type: number
      - required: false
        type: number
  utf8.codes:
    args:
      - type: string
  utf8.len:
    args:
      - type: string
      - required: false
        type: number
      - required: false
        type: number
  utf8.offset:
    args:
      - type: string
      - required: false
        type: number
      - required: false
        type: number